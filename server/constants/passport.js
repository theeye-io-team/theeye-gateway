
const PROTOCOL_LOCAL = 'local'
const PROTOCOL_OAUTH2 = 'oauth2'
const PROTOCOL_LDAP = 'ldap'

const PROVIDER_THEEYE = 'theeye'

module.exports = {
  PROVIDER_THEEYE,
  PROTOCOL_LOCAL,
  PROTOCOL_OAUTH2,
  PROTOCOL_LDAP
}
